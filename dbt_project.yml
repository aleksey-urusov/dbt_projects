# Project
name: dbt_try
version: "1.0"
config-version: 2
profile: dbt_try

## Sources
model-paths: [models]
analysis-paths: [analysis]
test-paths: [tests]
seed-paths: [seeds]
macro-paths: [macros]
snapshot-paths: [snapshots]

## Target
target-path: target
clean-targets: [target, dbt_modules]

## Models
models:
  dbt_project:
    staging:
      +materialized: view
    marts:
      +materialized: table
